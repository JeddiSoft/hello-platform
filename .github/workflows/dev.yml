name: DEV Delivery

on:
  push:
    branches:
      - develop
    paths-ignore:
      - "k8s/dev/deployment.yaml"

jobs:
  dev-delivery:
    uses: JeddiSoft/platform-workflows/.github/workflows/delivery-dev.yml@main
    permissions:
      contents: write
      packages: write
    secrets: inherit
