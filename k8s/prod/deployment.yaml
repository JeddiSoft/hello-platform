apiVersion: apps/v1
kind: Deployment
metadata:
  name: hello-platform
  namespace: hello-prod

spec:
  replicas: 3

  selector:
    matchLabels:
      app: hello-platform

  template:
    metadata:
      labels:
        app: hello-platform

    spec:
      containers:
        - name: hello-platform
          image: ghcr.io/jeddisoft/hello-platform:efd206d6df205e2b9383e8060a88532052ecc1c0

          ports:
            - containerPort: 8000

          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"

          readinessProbe:
            httpGet:
              path: /health
              port: 8000

          livenessProbe:
            httpGet:
              path: /health
              port: 8000